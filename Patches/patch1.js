// patches should have a few things in here

// should require some sort of pre-req, said patches should be done first then this file can be invoked

// here should be an SQL transaction that when pre reqs have been completed to
// then would assign to SQL string variable and passed to server/middleware to then perform patch 